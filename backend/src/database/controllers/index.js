const {
    Addresses,
    AddressCities,
    AddressCountries,
    AddressLocations
} = require('./addresses');

const {
    Mails,
    MailSenderNames
} = require('./mails');


module.exports = {
    Addresses,
    AddressCities,
    AddressCountries,
    AddressLocations,
    Mails,
    MailSenderNames
};