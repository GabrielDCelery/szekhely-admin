const {
    Addresses,
    AddressLocations,
    AddressCities,
    AddressCountries
} = require('./addresses');

const {
    MailSenderNames
} = require('./mails');

module.exports = {
    MailSenderNames,
    Addresses,
    AddressLocations,
    AddressCities,
    AddressCountries
};